# Repos whose docs/ directory is published as part of this site.
# slug is the path it mounts at, and must also appear in website/content/_meta.js.
- repo: rammp-org/sheppy
  slug: sheppy
  title: sheppy
  assets: [install.sh]
  exclude: [superpowers, AGENTS.md, CLAUDE.md]
- repo: rammp-org/kinova-gen3-ros2
  slug: kinova-gen3-ros2
  title: Controlling the arm
  # Beyond the default exclusions (superpowers, AGENTS.md, CLAUDE.md): the
  # on-robot runbook is an operational record, not user-facing docs. It stays
  # in the repo.
  # NOTE: overriding `exclude` REPLACES the default list, and compose.mjs errors
  # on a pattern that matches nothing -- so AGENTS.md/CLAUDE.md are omitted here
  # because this repo's docs/ has neither, and the dated handoff notes were
  # dropped from this list when the repo removed them.
  exclude:
    - superpowers
    - on-robot-runbook.md
- repo: rammp-org/rammp-teleop
  slug: rammp-teleop
  title: Teleoperating the arm
  # docs/superpowers holds the design spec and plan; not user-facing.
  exclude: [superpowers]
- repo: rammp-org/rammp-rtps
  slug: rammp-rtps
  title: Embedded interfaces (RTPS)
  # docs/ holds only user-facing pages, none of the default exclusions, and
  # compose.mjs errors on a pattern that matches nothing.
  exclude: []
