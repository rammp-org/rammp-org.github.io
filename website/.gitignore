node_modules/
.next/
out/
next-env.d.ts
.sources/
# Composed sources land under content/<slug>/ and public/<slug>/. The hub's
# own directories must be negated below, or new files in them are silently
# ignored by `git add`.
content/*/
!content/contributing/
public/*/
