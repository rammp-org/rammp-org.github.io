---
title: Controlling the base
---

# Controlling the base

Coming soon. The base driver and its ROS 2 interface do not exist yet. When
they do, this section will document them the way
[Controlling the arm](/kinova-gen3-ros2) documents the arm, and the base's
message types will live in
[`rammp_base_interfaces`](/interfaces/base).

Below the ROS 2 layer, the MIB drives each wheel's PACE RACER motor controller
over RTPS. That contract is documented in
[Embedded interfaces (RTPS)](/rammp-rtps/RACER-MOTOR-API).
